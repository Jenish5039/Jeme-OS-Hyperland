-- Jeme OS Monitor Configuration Template
-- Copy to ~/.config/hypr/monitors.lua and edit as needed.

-- Primary Display
hl.monitor({
    output = "eDP-1",             -- Change to your monitor name (e.g. DP-1, HDMI-A-1, eDP-1)
    mode = "1920x1080@144.0",     -- Resolution and refresh rate (or "preferred")
    position = "0x0",             -- Offset coordinates
    scale = 1.0                   -- Fractional or integer scaling factor
})

-- Optional Secondary Display (Uncomment if needed)
-- hl.monitor({
--     output = "DP-1",
--     mode = "2560x1440@165.0",
--     position = "1920x0",
--     scale = 1.0
-- })
